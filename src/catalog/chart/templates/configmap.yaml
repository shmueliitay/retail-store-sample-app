apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "retail-store-catalog.fullname" . }}-config
  labels:
    app.kubernetes.io/name: {{ include "retail-store-catalog.name" . }}
data:
  {{- range .Values.config }}
  {{ .name }}: {{ .value | quote }}
  {{- end }}

