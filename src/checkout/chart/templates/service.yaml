apiVersion: v1
kind: Service
metadata:
  name: {{ include "retail-store-checkout.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "retail-store-checkout.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app.kubernetes.io/name: {{ include "retail-store-checkout.name" . }}

