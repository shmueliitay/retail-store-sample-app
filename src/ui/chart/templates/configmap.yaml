apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "retail-store-ui.fullname" . }}-config
  labels:
    app.kubernetes.io/name: {{ include "retail-store-ui.name" . }}
data:
  {{- range .Values.config }}
  {{ .name }}: {{ .value | quote }}
  {{- end }}

